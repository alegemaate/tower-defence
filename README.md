# TowerDefence

Tower defence game built using asw and EnTT.

## Setup

### CMake

```bash
cmake -B build .
cmake --build ./build
```

### Build Emscripten

```bash
emcmake cmake -B build .
cmake --build ./build
```
